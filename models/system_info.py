class SystemInfo:
  def __init__(self, hostname, username, os_name, architecture):
    self.hostname = hostname
    self.username = username
    self.os_name = os_name
    self.architecture = architecture